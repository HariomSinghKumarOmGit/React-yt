import {Card, card } from '../Components/card';
import {Nevbar} from '../Components/Nevbar';


const App = () => {
  return (
    <div>
       <Nevbar/>
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
      {card()} 
       {Card()}
    </div>
  );
  //  <div>
  //   {ola()}
  //    {Card()}
  //  </div>
  
}

export default App
